/**
 * The EventsDroppedReason enumeration contains a set of values that specify the reason for dropping an event.
 */
export const EventsDroppedReason = {
    /**
     * Status set to non-retryable.
     */
    NonRetryableStatus: 1,
    /**
     * The event queue is full.
     */
    QueueFull: 2
};
