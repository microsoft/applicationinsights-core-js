language: node_js
node_js:
  - "10.9"
before_install:
  - echo "registry=https://pkgs.dev.azure.com/mseng/_packaging/ApplicationInsights-Team/npm/registry/" > .npmrc
  - echo "" >> .npmrc
  - echo "always-auth=true" >> .npmrc
  - echo "" >> .npmrc
  - echo "//pkgs.dev.azure.com/mseng/_packaging/ApplicationInsights-Team/npm/registry/:_authToken=\${NPM_TOKEN}" >> .npmrc
  - echo "" >> .npmrc
  - echo "//pkgs.dev.azure.com/mseng/_packaging/ApplicationInsights-Team/npm/:_authToken=\${NPM_TOKEN}" >> .npmrc
  - cp amd/package.json .
before_script:
  - npm install grunt-cli -g